// Footer Component JS
(function() {
    function initFooter() {
        console.log('Footer initialized');
        
        // Add footer-specific functionality if needed
        const footer = document.querySelector('.footer');
        if (footer) {
            // Footer is ready
            console.log('Footer section loaded');
        }
    }

    // Initialize immediately when script loads
    initFooter();
})();
